# Mock `InstaWear-design-from-zero` vs Actuel `instawear_gem` — Fonctionnalités majeures manquantes (frontstore)

**Périmètre :** fonctionnel, pas style/couleur. Mock = données mock `src/data/*` + `localStorage`, Actuel = Supabase/Stripe live.

**Légende (4 niveaux — le 6-niveaux proposé se recouvrait : ✅🔁≈🔁, 🟠≈❌) :** ✅ Fait (conforme au mock) · ✅✅ Fait mieux que le mock · 🔁 Partiel (reste précisé dans la cellule) · ❌ Reste à faire.

| #      | Fonctionnalité mock majeure                                                                                                                                                                                                             | Actuel                                                                                                                                                                                                                                                                             | Statut       |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| **1**  | **Avis & commentaires produit** `ProductReviews.tsx` + `useMyReviews.ts` : note moyenne `avgScore`, badge `vérifié`, `verified` lié à achat, CRUD `add/update/delete` par `userId`, tri, pagination                                     | Actuel : dépôt + affichage + `verified` + utile + mes avis (compte) OK ; manque éditer/supprimer ses avis, pas de `useMyReviews` | 🔁 Partiel (reste : éditer/supprimer ses avis — dépôt + `verified` + utile + mes avis OK) |
| **7**  | **Multi-devises / Ship-to** `useCurrency` + `currency.ts` (`rateFromEur` fixe) + `country→code` affiché `EUR/CHF/CAD` à côté du `ship`                                                                                                  | Actuel : `useCurrency` ajouté mais prix restent `EUR` sans conversion `rateFromEur`, `Header` ship dropdown présent mais `Cart/Checkout` utilisent `useCurrencySymbol` (EUR fixe)                                                                                                  | 🔁 Partiel (reste : appliquer `rateFromEur` — taux CHF/CAD définis, jamais utilisés) |
| **8**  | **Catalog URL partageable** `parseFiltersFromSearch/serializeFiltersToSearch` pour `event/cat/q/style/material/pmin/pmax/stock/size/color/sort/view` + `replaceState` (pas d'historique)                                                | Actuel : `CatalogSection` filtre via `App` `searchTerm/selectedCategory` sans sérialisation URL ; `replaceState` seulement pour `/produit/:id` et pages, pas pour filtres prix/taille/couleur                                                                                      | ❌ Reste à faire (sérialisation filtres catalogue en URL) |
| **10** | **Infinite scroll + skeletons** `PAGE_SIZE 12` + `IntersectionObserver rootMargin 400px` + `isLoadingMore 250ms` + `ProductCardSkeleton/ProductPageSkeleton`                                                                            | Actuel : `CatalogSection` a `PAGE_SIZE 12` + `IntersectionObserver` + skeletons, `ProductPageSkeleton` créé mais `ProductPage` n'a pas de `isLoading 350ms`                                                                                                                        | 🔁 Partiel (reste : skeleton/`isLoading` sur `ProductPage` ; catalogue OK) |
| **16** | **Auth complet** `AuthModal` `login/signup/OTP/reset` avec `DOB`, `newsletter` checkbox, OTP 6-digit `5min TTL`, `password validation`                                                                                                   | Actuel : `AuthModal` V1 `login/signup/resetPassword` Supabase (pas d'OTP, pas de `DOB`)                                                                                                                                                                                             | 🔁 Partiel (reste : OTP / DOB) |
| **19** | **SEO/PWA** `usePageMeta` dynamique par page + `site.webmanifest` + `og/twitter/schema Organization` + `theme-color`                                                                                                                     | Actuel : `usePageMeta` créé mais jamais appelé, manifest + og + `theme-color` + sitemap OK, pas de `SearchAction`                                                                                                                                                                                | 🔁 Partiel (reste : brancher `usePageMeta` dynamique ; manifest/og/`theme-color`/sitemap OK) |

**Conclusion (MAJ vérifiée au code le 22/09/2026)** : 12/19 ✅/✅✅ (dont 4 mieux que le mock : guides tailles Printful réels, facettes calculées, suivi RPC réel, `/recherche` routée). Reste : **éditer/supprimer ses avis (1), conversion devise `rateFromEur` (7), URL filtres catalogue (8), skeleton PDP (10), OTP/DOB (16), `usePageMeta` dynamique (19)** — 5 🔁 + 1 ❌.
