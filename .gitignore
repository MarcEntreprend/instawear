node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
Models/
*.zip
instawear-security-check.zip
# Docs personnelles (bloc-notes + backlog) : jamais commitées
blocNote.md
database-context-100926.md
Fonctionalités-majeures-manquantes-(frontstore).md
audit-interface-admin.md

.vercel


# Docs personnelles (audits, notes dev)
docs-front-back/

# Générés à chaque build (prebuild sitemap + scripts prerender) : turnover
# catalogue permanent (ajouts/retraits produits) — commiter ne produirait que
# du bruit. Régénérés via `npm run build` en local comme sur Vercel
# (buildCommand par défaut -> prebuild). Note : sitemap.xml plafonne à
# 50 000 URLs (suffisant pour des dizaines de milliers ; au-delà : index).
public/sitemap.xml
public/llms.txt
