# Mock `InstaWear-design-from-zero` vs Actuel `instawear_gem` — Fonctionnalités majeures manquantes (frontstore)

**Périmètre :** fonctionnel, pas style/couleur. Mock = données mock `src/data/*` + `localStorage`, Actuel = Supabase/Stripe live.

| #      | Fonctionnalité mock majeure                                                                                                                                                                                                             | Actuel                                                                                                                                                                                                                                                                             | Statut       |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| **1**  | **Avis & commentaires produit** `ProductReviews.tsx` + `useMyReviews.ts` : note moyenne `avgScore`, badge `vérifié`, `verified` lié à achat, CRUD `add/update/delete` par `userId`, tri, pagination                                     | Actuel : `ratings {score,count}` affiché seul, pas de dépôt/édition/suppression par client, pas de `verified`, pas de `ProductReviews`                                                                                                                                             | **Manquant** |
| **2**  | **Recently Viewed** `useRecentlyViewed` (queue 12 ids `localStorage`) + `RecentlyViewedSection` horizontal scroller sur PDP (exclut courant)                                                                                            | Actuel : hook créé `src/hooks/useRecentlyViewed.ts` mais jamais branché sur `ProductPage`/`openProduct`, pas de section                                                                                                                                                            | **À moitié** |
| **3**  | **Frequently Bought Together** `getFrequentlyBoughtTogether` (même `event` catégorie différente, déterministe hash) + `FrequentlyBoughtTogether.tsx` kit cases à cocher + prix total                                                    | Actuel : non porté, `ProductPage` montre seulement `You may also like` (même catégorie)                                                                                                                                                                                            | **Manquant** |
| **4**  | **Zoom & Lightbox** `ZoomImage.tsx` (hover loupe + `requestLightbox`) + `ImageLightbox.tsx` (fullscreen swipe, dots) + thumbnails `sm:vertical 64px`                                                                                    | Actuel : `ProductPage` utilise `ImageZoom` simple (V1), pas de loupe, pas de lightbox plein écran                                                                                                                                                                                  | **À moitié** |
| **5**  | **Guide des tailles par catégorie** `SizeGuideModal.tsx` + `sizeGuides.ts` (apparel vs accessories, tableau mesures, image) déclenché icône `Ruler`                                                                                     | Actuel : `ProductPage` a `SizeGuideDisplay` générique `inch` + fallback S/M/L/XL cm, pas de modal par catégorie, pas de `sizeGuides.ts`                                                                                                                                            | **À moitié** |
| **6**  | **Deal Countdown live** `DealCountdown.tsx` `setInterval 1s` + `compact` sur carte vs pleine sur PDP/Promotions, `isDealLive()`                                                                                                         | Actuel : `StoreProductCard` affiche `countdownStr` passé par `App` (statique), pas de `DealCountdown` live autonome                                                                                                                                                                | **À moitié** |
| **7**  | **Multi-devises / Ship-to** `useCurrency` + `currency.ts` (`rateFromEur` fixe) + `country→code` affiché `EUR/CHF/CAD` à côté du `ship`                                                                                                  | Actuel : `useCurrency` ajouté mais prix restent `EUR` sans conversion `rateFromEur`, `Header` ship dropdown présent mais `Cart/Checkout` utilisent `useCurrencySymbol` (EUR fixe)                                                                                                  | **À moitié** |
| **8**  | **Catalog URL partageable** `parseFiltersFromSearch/serializeFiltersToSearch` pour `event/cat/q/style/material/pmin/pmax/stock/size/color/sort/view` + `replaceState` (pas d'historique)                                                | Actuel : `CatalogSection` filtre via `App` `searchTerm/selectedCategory` sans sérialisation URL ; `replaceState` seulement pour `/produit/:id` et pages, pas pour filtres prix/taille/couleur                                                                                      | **À moitié** |
| **9**  | **Filtres complets** `FilterPanel` : `EVENT(5) + CATEGORY(4) + Prix range + Taille XS-XXL + OneSize + Couleur dots + En stock toggle` + `STYLE(7)` + `MATERIAL(5)`                                                                      | Actuel : `CatalogSection` a `event/cat/prix/taille/couleur/stock` mais pas `style/material`, `SORT` 5 options manquent, `COLOR_LIST` 8 vs mock `COLOR_LIST` complet                                                                                                                | **À moitié** |
| **10** | **Infinite scroll + skeletons** `PAGE_SIZE 12` + `IntersectionObserver rootMargin 400px` + `isLoadingMore 250ms` + `ProductCardSkeleton/ProductPageSkeleton`                                                                            | Actuel : `CatalogSection` a `PAGE_SIZE 12` + `IntersectionObserver` + skeletons, `ProductPageSkeleton` créé mais `ProductPage` n'a pas de `isLoading 350ms`                                                                                                                        | **À moitié** |
| **11** | **Sticky buy card + barre mobile** PDP 3-col `grid [1fr_0.85fr_320px]` avec carte `sticky` + `mobile fixed bottom` (`price + bag + Zap`) qui remplace `MobileTabBar`                                                                    | Actuel : `ProductPage` 3-col `45%/25%/25%` avec `bezel` sticky, pas de barre mobile fixe dédiée (utilise `MobileTabBar` générique)                                                                                                                                                 | **À moitié** |
| **12** | **Related slider** `RelatedProductCard` horizontal snap + `getRelatedProducts`                                                                                                                                                          | Actuel : `ProductPage` a `You may also like` 4x `StoreProductCard` grid, pas de slider snap                                                                                                                                                                                        | **À moitié** |
| **13** | **Compte 8 onglets** `AccountLayout` drawer `dashboard/orders/favourites/reviews/addresses/notifications/support/profile` avec `loyalty tier` (`totalSpent`), `reorder`, `default` adresse, `mark read/all`, `createInteraction + chat` | Actuel : `AccountPage` 6 onglets (`orders/saved/cart/notifs/profile/support`) + `dashboard/reviews` ajoutés en Phase 6 mais `dashboard` minimal (3 stats sans tier), `reviews` placeholder, `addresses` restée dans `profile` (pas d'onglet séparé), `support` sans chat thread V2 | **À moitié** |
| **14** | **Checkout 4 étapes dual** `PaymentMethodSelect` tuiles `Stripe Checkout #635BFF` vs `Carte` + `CreditCard 3D flip` (front/back sur `CVC focus`, Luhn, brand) + `FREE_THRESHOLD 35€` progress                                           | Actuel : `CartDrawer` a progress bar, `CheckoutFlow` a `CreditCardPreview` flip ajouté Phase 5 mais tuiles paiement restent V1 (`Stripe Elements` seul), pas de `PaymentMethodSelect`                                                                                              | **À moitié** |
| **15** | **Stock fin** `stockQuantity` 4-52 + `Flame Plus que N` ≤10 / `Check En stock` / `Épuisé` + cap `max`                                                                                                                                   | Actuel : `StoreProductCard` affiche `In stock` générique via `availability`, pas de `stockQuantity` chiffré                                                                                                                                                                        | **Manquant** |
| **16** | **Auth complet** `AuthModal` `login/signup/OTP/reset` avec `DOB`, `newsletter` checkbox, OTP 6-digit `5min TTL`, `password validation`                                                                                                  | Actuel : `AuthModal` V1 `login/signup/resetPassword` Supabase (pas d'OTP, pas de `DOB`)                                                                                                                                                                                            | **À moitié** |
| **17** | **Recherche instantanée** `Header` `normalizeText` accent-insensible + suggestions `catégories(4) + produits(5)` avec icônes/thumbnails, `onMouseDown preventDefault`                                                                   | Actuel : `Header` V2 `SuggestionsList` groupé ajouté Phase 1, mais `App` search reste `filter by title` simple `toLowerCase` sans `normalize` ni `EVENT_TYPES/CATEGORIES`                                                                                                          | **À moitié** |
| **18** | **Pages standalone** `SearchResultsPage` (`q` + `EVENT_TYPES` chips), `OrderTrackingPage` (`DEMO_ORDERS` + `stepper`), `LegalPage` 3 docs `dompurify`, `ContactPage` async stub                                                         | Actuel : `SearchResultsPage/OrderTrackingPage` créés Phase 7 (adaptés Supabase) mais `Header search` ne navigue pas vers `/recherche` (filtre catalogue), `OrderTrackingPage` utilise `get_order_tracking` RPC (pas de `DEMO_ORDERS`)                                              | **À moitié** |
| **19** | **SEO/PWA** `usePageMeta` dynamique par page + `site.webmanifest` + `og/twitter/schema Organization` + `theme-color`                                                                                                                    | Actuel : `usePageMeta` créé mais jamais appelé (audit), pas de `manifest` PWA, pas de `SearchAction`                                                                                                                                                                               | **Manquant** |

**Conclusion** : mock apporte ~19 briques fonctionnelles ; actuel en a porté ~60% en visuels (Phases 0-7) mais garde la logique Supabase live. Manquent surtout : **avis CRUD, RecentlyViewed/FrequentlyBought, Zoom/Lightbox, guide tailles, conversion devise, URL filtres complète, sticky buy, slider related, compte 8 onglets complet, OTP**.
